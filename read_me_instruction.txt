this script goes through a directory and to process all the text or log file it finds.
input is the path to the directory.
outputs a .txt file containing all data extracted. 

instructions 


-go to the settings.txt and input the path there then run the script